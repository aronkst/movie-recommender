<div class="mt-5 grid grid-cols-1 md:grid-cols-2 2xl:grid-cols-4 gap-5">
  <% movies.each do |movie| %>
    <%= render movie %>
  <% end %>
</div>

<div class="mt-5 grid">
  <%= paginate movies %>
</div>
