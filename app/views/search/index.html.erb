<h1 class="text-4xl">Search</h1>

<%= render partial: "filter/search" %>

<div class="mt-5 grid grid-cols-1 md:grid-cols-2 2xl:grid-cols-4 gap-5">
  <% @movies.each do |movie| %>
    <%= render partial: "search/movie", locals: { states: @states, movie: movie } %>
  <% end %>
</div>
