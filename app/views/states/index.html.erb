<h1><%= params[:state] %></h1>

<%= render partial: "filter/filter", locals: { route: states_index_path(params[:state]) } %>

<% @movies.each do |movie| %>
  <code><%= movie.to_json %></code>
  <hr />
<% end %>

<%= paginate @movies %>